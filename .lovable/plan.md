

## Accordéons fermés par défaut

Changement minimal dans `src/pages/ProjectSettings.tsx` ligne 172 : remplacer `defaultValue={["user-flow", "page-check", "ui-element"]}` par `defaultValue={[]}` pour que toutes les sections soient fermées au chargement.

